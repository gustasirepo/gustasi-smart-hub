User-agent: *
Allow: /
Disallow: /private/
Disallow: /admin/
Disallow: /api/
Disallow: /*?*  # Avoid duplicate content from query parameters
Disallow: /*/print$  # Avoid print versions of pages

# Sitemap location (update with your domain)
Sitemap: https://yourdomain.com/sitemap.xml

# Crawl-delay: 10  # Uncomment and adjust if needed to limit crawl rate
